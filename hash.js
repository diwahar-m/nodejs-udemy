// const bcrypt = require('bcryptjs'); 
// // no. of times we run algorithm to generate the salt. 
// async function run(){ 
//     // more number means more complex. default is 10
//     const salt = await bcrypt.genSalt(10); 
//     const hashed = await bcrypt.hash( '1234', salt )
//     console.log(salt)
//     console.log(hashed)
// } 
// run()